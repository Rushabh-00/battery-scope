# BatteryScope release rules.
# AndroidX/Compose publish their own consumer rules; keep this file intentionally minimal
# so R8 can remove unused application code and resources without unnecessary keep rules.
